# ==============================================================================
# NoteApp Backend - Local Development Configuration (EXAMPLE)
# ==============================================================================
# Copy this file to .env and configure with your actual values
# This file is used when running Django OUTSIDE Docker (local development)
# For Docker Compose, use the root .env file instead
# ==============================================================================

# ==============================================================================
# Django Configuration
# ==============================================================================
# SECURITY WARNING: Generate a new secret key!
# Generate with: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=your-secret-key-here-generate-a-new-one

# Debug mode (always True for local development)
DEBUG=True

# Allowed hosts (comma-separated, no spaces)
ALLOWED_HOSTS=localhost,127.0.0.1

# ==============================================================================
# Database Configuration
# ==============================================================================
# Option 1: Connect to PostgreSQL running in Docker
DATABASE_URL=postgresql://noteapp_user:your_password@localhost:5432/noteapp_db

# Option 2: Connect to local PostgreSQL (uncomment and adjust if needed)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/noteapp_local

# ==============================================================================
# Redis Configuration
# ==============================================================================
# Connect to Redis running in Docker or locally
REDIS_URL=redis://localhost:6379/0
CELERY_BROKER_URL=redis://localhost:6379/1

# If Redis has a password (uncomment and adjust)
# REDIS_URL=redis://:your_password@localhost:6379/0
# CELERY_BROKER_URL=redis://:your_password@localhost:6379/1

# ==============================================================================
# JWT Token Configuration
# ==============================================================================
# Access token lifetime in minutes (default: 15 minutes)
JWT_ACCESS_TOKEN_LIFETIME=15

# Refresh token lifetime in minutes (default: 10080 = 7 days)
JWT_REFRESH_TOKEN_LIFETIME=10080

# ==============================================================================
# CORS Configuration
# ==============================================================================
# Allowed origins for CORS (comma-separated, no spaces)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ==============================================================================
# Development Tips
# ==============================================================================
# To run PostgreSQL and Redis via Docker for local development:
#   docker-compose up postgres redis
#
# Then run Django locally:
#   cd backend
#   poetry shell
#   python manage.py runserver
#
# And Celery worker (separate terminal):
#   celery -A api worker --loglevel=info
